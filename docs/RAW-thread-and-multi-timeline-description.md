I like you to investigate the current stream workspace, stream engine, and timeline system, time code system, navigation system, queue triggering system, and play, pause, resume mechanism ETC as a whole. Effectively, the current implementation state is largely functioning, but there are many edge cases that are problematic and seem to intertwine with the entire architecture and how to address one by one without introducing other implications. And from a broader angle, the current implementation covers phases one to seven in the stream workspace and runtime plan without touching on the flow mode. I'd like to use this opportunity to both specify the requirement further for flow mode, as well as redesign or refactor the stream engine that builds upon the current system, but take this opportunity of building the flow mode to tidy up the current edge case problems.

One of the main problems we have now is between the navigation mechanism scene by scene in the stream in a linear way to continue playback, to be compatible with the phenomenon and cases in which the users use the direct run from here feature of each scene, and how that differentiates between different cases. Depending on the nature of that particular scene that is run directly from here and depending on the current playback engine status, whether it's paused, reset, or running also, and so that there is this bi-directional influence on the stream scene progression level while at the same time, we have another layer of mechanism that hovers around the time code and the. Estimated composed complete timeline for a stream that interferes with some of the triggering cases and causes the stream playback to behave awkwardly in some edge cases, although indeed the current system at large does work as we expected

So to systematically update the architecture and solve this problem, I like to sort of introduce another layer of abstraction in the stream. So currently there are subcue, scene, and stream three levels of organization. I like to introduce the force organization level that is not exposed to the user, but use for the program to battle to better handle the sequence and playback timeline as well the new abstraction level, I like to call it as a thread which consists which consists of multiple scenes and a stream can consist multiple threads.

The organization of a thread is actually a tree structure. The starting point of a thread is a scene that is not triggered by scene-following triggers (follow start or follow end), Currently they include manual trigger or at time code trigger, (but later on we may support other) We will call these triggers as operation triggers, and call (follow start or follow end) as auto triggers. Then to map out a complete tree structure for thread, we should both start probing from an operation trigger and exhaustively see what are the scenes with auto triggers that follows it that forms a tree, or probe backwards from all auto triggered scenes to identify its starting scene in the thread. (We will make the algorithm decision later to choose the most robust + efficient way). Naturally, there could be multiple threads in a stream that starts from different scenes of operational triggers, and each threadss could have multiple branches, as there could be multiple scenes following the same and only some of them are followed by later auto triggered scenes, and so on and so forth. 

Then the question left is how should the thread level structure compatible with the linear list or timeline? That is the key mechanism we need to think of and I have a tentative plan that I'm going to tell you, but you' are welcome to also, um, give your opinion on if we can make it even better. So the idea is the timeline of the stream should be always dynamically composed and calculated given the order and length of the threads, and of course, within each thread there are lengths of scenes, and within each scene there are lengths of sub cus that we can inherit from the current system in terms of calculating on their length. So the default timeline composition order derived from all the threads should refer to the relative order of the scenes in the linear list, where by scenes I mean the scenes that act as the starting point of each thread. So we consider their relative position in the linear list, and then we consider threads to follow one and each other given the order of their starting scene. And then in terms of the len of a particular thread, we consider the longest branch in that thread. As a result, the default total length of an entire stream should be effectively calculated by summing up and ordering the longest branch in each thread and connect them together

Okay, so the reason I said that this is only the default calculation of the order and length of the entire stream is because it is totally possible that during playback, the actual desired order of launching different threads could vary depending on user interaction given our current pausing mechanism. So the pausing mechanism goes like this, both currently and hopefully also in the future, although currently there is no unofficial level of abstraction of thread, but effectively what happens now is when all the auto triggered scenes are finished and all that left or operational operationally triggered scenes, the main playback engine and timeline should pause and wait for the user to manually trigger whatever they want later on, and that is effectively equivalent to triggering a new thread in our new system. 

So obviously there will be two cases, one is that after the previous thread has ended and the playback engine is paused, the user goes directly click on play from playback focus button in the header as the playback focus should also advance to the beginning scene of the immediately following thread, the immediately following thread should start playing, timeline restore, and there is no change in timeline calculation or threads reordering.

However, there could be another case where the user choose to double click on a scene that belongs to a thread that does not immediately follow the previously ended thread, in the default order and calculation. And then when user chooses to play whether through first double clicking to set the playback focus and play from playback focus or use the run from here function of that particular scene, it actually concerns two sub cases: one is that this scene is the beginning scene of another thread, and the other case is that this scene is a middle scene of another thread. In both cases, we will need to reorder the threads in the timeline calculation, and to move the particular thread the user launch to play to attach to the end of the previously finished thread. However, the total duration of the entire stream calculation based on the threads should not change as there is only this reordering. (similar to the current system implementation, whenever a subcus lens is changed, a scenes lens is changed, and now a thread lens is changed, the entire timeline should recalculate based on the latest duration, so that the duration calculation and all the calculation should decouple from each other)

Of course, what I described just now until applies to the case when a user behaves very manually and only wait until the previous thread is completely finished, and then start to use operational triggers to trigger the other threads. However, there could be another main case where there is a previous thread that is running, but then the user triggered something in another thread manually, whether it's to trigger the first scene of another thread or trigger a middle scene of another thread. This is when both the ordering and timeline Les duration calculation both need dynamically update. In the previous mechanism we just described, we assume that threads are played in series, although scenes within different threads can play in parallel, But we assume that a thread follows one another, but obviously in real life is not the case. So our initial default timeline calculation indeed follow this assumption that all threads are played in serial in a sequence and there is no parallel case existing, But we should prepare our architecture for parallel running, and that's when we will need to introduce multiple virtual timelines beyond the current main playback timeline and edit timeline because essentially, although there is this timeline recalculation and propagation mechanism in the project, we still treat the playing timeline as unified, but essentially there could be different time lines in the actual new system

Now refer to the image I give you, which is an illustration of three threads with different lengths and where threads b has two branches, and it' is clearly that the blue dotted line that connected the longest branch of each thread is the default timeline, and it is also the timeline of which, if the thread follows runs in sequence that the previously described reordering case should happen on as the total Les didn't change. For example, if I do not trigger thread B after thread A but trigger thread C after thread A, it only switch the order of thread B and c, but the total length of the timeline is still fixed

However, if during thread A or thread B is playing on the main timeline, the user choose to launch thread C whenever they want, whether it's launching the first scene in thread C or a scene in the middle of thread C, it should effectively launch another timeline that runs parallel with the first main timeline, so that the total length of the main timeline now needs update as thread C is removed from the first timeline. And the first timelines total length should now only be the sum of thread A and thread B, and the new timelines total length should be the total length of thread C. And if the user triggers thread C from the first scene, then the new timelines, playback position and time codes start with zero of course, but if the new user choose to trigger a scene in the middle of thread C, then once upon the new timeline is initiated and launched, it should directly go start playing from the time code where the user selected scene is situated in the new timeline

And there is even another case. Let's say there are a false thread that is not available on the screen called thread D that supposedly follows thread C in the default calculator timeline in the order. Although its ma in terms of the triggering mechanism, it does not quote unquote follow thread C as it is also operationally triggered. In that case, when thread C is launched and spawned in a new timeline, thread D should move forward in the original timeline placed after thread B, so that the total length of the main first time line should be calculated as a sum of thread A, B and D, then it should also similar to thread C, spawn its own timeline as the third timeline that runs parallel with the others

Now in a further advanced case, let's say thread A is already finished playing and thread B still sits in the main timeline and is currently playing no matter which scene it is currently playing, but at the same time thread C and D are also running in parallel timelines. And at this moment the user to decided to re trigger a scene from thread A that is marked completed already in the stream. In this case the main timeline calculation should not be refresbed, As thread B is still playing on the main timeline and following thread B, there might be a thread E that used to follow thread D but now shifted to follow immediately thread B as thread D is shifted to another timeline. In that case, when a user choose to relaunch a scene from thread A, what effectively what effectively should happen is that we make a copy of the thread A and launch it on a new timeline; and the main timeline duration and order should not change in this case.

Now in terms of pausing beyond the current pausing mechanism, obviously you need to also adapt to the new multi thread and multi timeline system Effectively if there is only one timeline running at the moment, then the pause should function more or less the same as the current implementation in terms of the two resume modes that we have in the project in the system. And then in terms of how the playback focus is, where the main transport playback button used as reference and how the playback focus updated automatically with the stream progresSiOn and the use a double click, that's all that needs to be keep kept as a bottom line. However, if there are multiple timelines running at the same time and effectively in the timelines that are launched later on beyond the main timeline there are different threads running, the pause will need to be carefully calculated and traced across all the timelines. 

Another thing to track is the playback focus as I am not 100% sure how it is functioning and being updated now in the system, but ideally in the future system it should be decoupled with timeline ordering and calculation as well as decoupled from timeline resetting ETC, effectively it is only an indicator to show from which scene that belongs to which thread will the playback launch, if the user clicks the main play button in the header transport

For example, let's say the entire screen is resetted and every thread and every scene is in ready state and nothing is played also, and the user directly go to one of the scenes in thread B and starts playing it. In that case there should be no extra timeline launching or threads reordering. What should happen in that case is the Cursor in the timeline or time code, jump to where the play scene in thread B is in the default main calculated timeline and start playing onwards. And then at this moment, the playback focus should lively update itself when the scene progress from one to another, whenever there is a scene progresSiOn happening but not being updated every frame or every moment. So resultantly, if during the playback of a scene, the user double clicks on another scene, then it should set the playback focus to that scene, and then if the user clicks on the play button in the header transport, it should start play from that scene, belong to that thread, depending on where that scene or where that thread is to see whether we need reordering, recalculation or launch a new timeline or not as we previously described. The play from here button should also act effectively in the same similar way. It effectively should mean should should should be a combination of two action, that is double clicking on the scene role and to click the major play button. That's all the run from here button should do.

Now in the previous case we described where the user directly start to launch and play a scene from thread B, the timeline indeed progresses, jumps to the relative position in the default timeline and start to progress, but what should happen to the other scenes that is positioned at least and estimated to be ordered before that scene? That is what I'm going to describe now: For the thread A that is placed before thread B but essentially detached as there are different threads, all the scenes from thread A should still stay in ready state and to be triggered whenever the user wants, whether by launching the first beginning scene of thread A or manually trigger one of the other threads in the thread a. However, for the scenes in the thread B that is before this manually triggered scene in thread B, which means we are triggering a scene in thread B that is not the beginning scene of thread B, then all the scenes in thread B before that scene should be updated into skipped state. And furthermore, IF a user clicks on a scene in the thread B that is before this current running scene, whether it's skipped state or complete state, and the user want to re trigger it while the other scene in thread B is still running, what should happen is we should make a copy of thread B in a new timeline and launch from that operationally triggered scene.

As you may have noticed, all the cases we've described so far seem mostly fall into the case where the operational trigger is used interchangeably with the manual trigger, as it will make more sense, but effectively the add time code trigger, if it's triggered then, should also function in the same way, only that the problem left is which timeline and which time code should the at time code trigger rely on, and the obvious answer is the main default timeline we have. Despite that timeline may be reordered and recalculated during the playback. Although whenever the user attempted to assign a scene to be triggered at time code, we'd pop up a reminder for them to remind that the main timeline may be recalculated and reordered during a PRO playback due to their interaction with it, so that it may not be reliable if there is no external timeline or time code that is said to be referred to (and the project now is not yet to support external timecode).

Last but not least, as an additional note, currently the timeline of the patch workspace and the stream workspace is already detached and decoupled, and we'd like to keep it as such and make sure the new mechanism only affects the stream workspace but not the patch workspace

Final remark: When the user clicks back to the first scene, it should effectively reset all temporal additional timelines and restore the default timeline (order of threads, durations, etc.) and reset all scenes' state

Final final remark: The manual seeks using the main global time range rail in the header should always apply to the main default timeline's latest ordered version. Seeking a position should refresh displays and audio outputs to surely set to that moment of playback on the default timeline.

---

That's so much for the mechanism and behavioral description of the new system. I know it's very complicated and consists of a lot of use cases and attach cases. So what I want you to do is to first clearly track all the state, all the concept and all the cases I've described, Use clear clearer language and sequential and conditional structure to track those cases as a benchmark for testing and development, and then you can start to generalize and abstractify the principles for the system and the orchestrations beyond the specific use cases, but in principle is could be compatibly supporting simultaneously what I described the all the cases I described.

You should write a new comprehensive doc on the mechanism now, and later we still start to discuss the UI/UX update for the list mode as well as flow mode.

You may ask me any questions to clarify anything or make any architectural decisions.

---

Now we'd like to maintain an exhaustive list of additional colors that share the same vibe as the current steam token colors that is molandic but very from each other. And in the list mode we want all the scenes that belongs to the same thread. Calculated thread has the same shared background shade color among those among the particularly maintained color list for threads, so that the user may visually identify which thread is which by looking at the color code as the scenes belongs to the same thread, may not necessarily close to each other in the linear list. 

For the current progress bar at the bottom edge of each scene role in the list, they should also render in the corresponding scene thread color code

The scene pill in the scene edit tab should also render it with a background shade of the belonging thread color.

For the current for the stream timeline rail - the full width timeline rail in the stream workspace header, it also need some UI update. Essentially it should always dynamically reflect the state of the default main timeline and the position of the scrubber, and the time code should also reflect the Cursor position relative to that main timeline. In terms of UI the real background now is a dimmed line and the progress area on the line now is rendered in the in the theme color. What I want to change and implement as a mechanism is that we will calculate the dynamically calculate the main timeline and all the threads relative proportion on that timeline, so that we may render the global timeline rail with different segments that's shaded with the corresponding color code of those threads. And the color tone should be persistent both in the rail background and in the progressed lighted up foreground, only that the background is the dimmed color code and the foreground is the bright verSiOn of the color code and the foreground should also reflect and respect the segmentation, proportional segmentation of the entire role according to the thread.

Now for the flow mode: I hope you can see that this new thread and the tree structure it has, with the multi timeline architecture, naturally maps to what we would expect for a flow mode in terms to visualize the threads and their orders on a canvas. 

I'll first paste the original brief flow mode design section with some update:

BG:
Denser grid than the current placeholder

Scene cards:
- resizable
- show scene number, title, and the duration as bottom line; indicate state with styling
- render previews for visual sub-cues in a dynamic grid within the card, by default pause for live sources
- render progress along the bottom edge when running
- show virtual links derived from trigger policies
- on hover, show centered play/pause toggle icon and Edit icon, two icons side by side
- on right-click, show options: duplicate, disable, remove
- edit icon selects the card and opens Scene Edit

Link rules:
- auto trigger link to the following scene
- `manual` and `at-timecode` do not link by default (as they are the beginning scene of a thread)
- missing followed scene references render as warning stubs

Canvas operations:
- pan
- zoom in/out
- fit to content (reset view)
- drag cards to reposition
- resize cards
- Add a scene by hovering outside the right edge of any existing card, click the add button to add a scene and by default follow the end of the hovered card scene
- Add a scene by right-clicking in empty space to call the menu containing the "Add Scene" option

Add-scene behavior:
- The new scene follows the hovered scene by default
- trigger defaults to `{ type: 'follow-end', followsSceneId: hoveredScene.id }`
- position defaults to the right of the hovered cards

Implementation:
- Use Rete.js for Flow mode.
- Persist card rects and Stream `flowViewport`.
- Keep the scene graph as Xtream-owned data; the library is the editing/view layer, not the source of truth.
- Require custom scene-card rendering, pan/zoom, selection, drag, resize, programmatic fit-view, custom links, and library-independent serialization.
- Use Rete area/pan/zoom and connection primitives, but keep scene cards, previews, hover controls, and command dispatch as Xtream-owned components.
- Add only the Rete packages required for the selected renderer path.
- Because the current control renderer is vanilla TypeScript, evaluate Rete Lit or classic renderer integration before introducing a React island.
- Treat Rete node and connection data as a projection of `PersistedSceneConfig.flow` and scene trigger policies.

Now, beyond these descriptions, here are the extra UI UX I'd like to design and implement:

1. First of all, for each scene card, the button metadata section should have the thread color-coded shade.

2. The top edge of the card should render the edit focus, like in the left end of list rows, and the playback focus border should also be implemented around scene cards.

3. The scene state should be indicated by style. Besides the normal style as ready, and running state with the progress bar below, disabled and skipped and complete state should have different levels of dim on the entire card - disabled is dimmest, and complete is lightest; paused should have a yellow-ish shade on the entire card like scene row in the list, failed should have a reddish shade, and preloading with a loading animation.

4. The default cards layout and layout reset

5. Timeline dotted curves and animation

Gant Chart mode for multi-timeline monitor (View only mode)